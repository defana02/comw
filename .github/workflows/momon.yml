name: momon
 
on: workflow_dispatch
 
jobs:
  build:
    runs-on: windows-latest
 
    steps:
    - uses: actions/checkout@v2
    - name: Initializing Setup
      run: ./setup.ps1
    - name: Starting CRD
      run: ${{ secrets.AUTHCODE }} -pin=${{ ecrets.PINCODE }}
    - name: Keep Alive
      run: ./timeout.ps1
